def lambda_notify_task_completed(task_id, task_title):
    # Simulate sending a notification
    print(f"Simulated AWS Lambda Notification: Task '{task_title}' with ID {task_id} has been marked as 'completed'.")
    # send mail to the user if required